name: bon_notifiers
description: "A collection of custom notifiers and notifier mixins that i use in my projects"
version: 0.0.1
github: https://github.com/Yoeri-z/bon_notifiers

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0